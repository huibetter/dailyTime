# Top Navigation and User Menu Implementation Plan

> Implemented plan for DailyTime navigation refactor.

**Goal:** Move account controls to the top-right and add Codex-style top-left workspace controls.

**Architecture:** Keep the existing single-page React state model and persist UI preferences/user state in localStorage. Reuse existing document actions through top menus.

**Tech Stack:** React, Vite, lucide-react, CSS.

**Spec:** User-requested DailyTime top navigation and user menu refactor.
