# 2026-09-03 修改历史

## 2026-09-03-14
- 修改人：Codex
- 修改摘要：将顶部设置下拉栏改为应用内独立设置页面，增加通用、外观和关于分类，保留本机配置、主题和紧凑模式的持久化设置。
- 影响范围：`src/main.jsx` 设置页面与导航状态；`src/styles.css` 设置页桌面端、窄屏和深色主题样式。

- 修改人：Codex
- 修改时间：2026-09-03-14
- 修改摘要：新增构建时 Git commit 注入，并在关于页面增加固定检查 GitHub `origin/main` 的更新按钮；通过 GitHub branches API 比较远程 `main` 提交，仅提示检查结果，不自动拉取或安装。
- 影响范围：`vite.config.js` 构建环境变量；`src/main.jsx` 版本信息和远程更新检查。

- 修改人：Codex
- 修改时间：2026-09-03-14
- 修改摘要：修复启动后界面空白问题，移除当前 `lucide-react` 版本未导出的 `Github` 图标导入。
- 影响范围：`src/main.jsx` 图标导入；恢复前端入口正常挂载。

## 2026-09-03-16
- 修改人：Codex
- 修改时间：2026-09-03-16
- 修改摘要：移除新安装时的示例项目和示例文档；新增项目行内删除、二次确认、最后项目删除后的空工作空间状态，并让桌面项目快照同步删除 SQLite 中已不存在的项目及其级联数据。
- 影响范围：`src/main.jsx` 初始化数据、项目删除和空状态；`src/data/runtime-storage.ts` 项目删除同步；`src/styles.css` 项目删除按钮和空状态样式。

- 修改人：Codex
- 修改时间：2026-09-03-16
- 修改摘要：新增 `app_settings` profile 读写接口；桌面端优先从 SQLite 加载本机用户配置，并将旧版 `localStorage` profile 首次迁移到 SQLite，浏览器端继续使用 `localStorage`。
- 影响范围：`src/data/runtime-storage.ts` 本机设置持久化；`src/main.jsx` 启动加载与保存逻辑；`README.md`、`docs/desktop-development.md` 数据说明。

## 2026-09-03-17
- 修改人：Codex
- 修改时间：2026-09-03-17
- 修改摘要：将顶部导航提升为横跨侧边栏和主内容的全局顶栏；新增“项目”下方的项目名称检索，并将顶部搜索限定为当前项目内的文档记录。
- 影响范围：`src/main.jsx` 项目筛选状态和搜索范围；`src/styles.css` 全局顶栏、侧边栏避让和项目搜索布局。

## 2026-09-03-18
- 修改人：Codex
- 修改时间：2026-09-03-18
- 修改摘要：将顶部四个工作区控制按钮统一移动到左上角，并将顶层导航高度缩减为 45px。
- 影响范围：`src/styles.css` 顶栏定位、主内容顶部偏移、时间线最小高度和移动端侧边栏避让。

- 修改人：Codex
- 修改时间：2026-09-03-18
- 修改摘要：修复 Tauri 发布包启动数据库加载失败时将空状态回写并覆盖本地数据的问题；仅在桌面数据成功加载后启用项目和文档持久化，加载失败时保留错误状态。
- 影响范围：`src/main.jsx` 桌面数据库初始化、水合标记、保存条件和错误提示。

- 修改人：Codex
- 修改时间：2026-09-03-18
- 修改摘要：串行化桌面端 SQLite 写入，避免新增项目与文档同时保存时触发外键竞态导致文档未落盘。
- 影响范围：`src/data/runtime-storage.ts` 项目、文档和本机设置写入队列。

- 修改人：Codex
- 修改时间：2026-09-03-18
- 修改摘要：修复 Tauri SQL 写入权限缺失导致安装包无法持久化的问题，增加 `sql:allow-execute`；移除数据库错误时永久停留在错误提示页的逻辑，保留未成功水合时禁止空数据回写的保护。
- 影响范围：`src-tauri/capabilities/default.json` SQL 权限；`src/main.jsx` 桌面数据库启动状态和错误处理。

## 2026-09-03-16
- 修改人：Codex
- 修改时间：2026-09-03-16
- 修改摘要：修复侧边栏项目名称在窄宽度下按字符竖排的问题，项目名称改为单行省略显示，项目数量和悬浮删除按钮保持横向排列。
- 影响范围：`src/styles.css` 项目行 flex 布局、文本溢出和删除按钮尺寸约束。
