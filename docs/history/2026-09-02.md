# 2026-09-02 修改历史

## 2026-09-02-00
- 修改人：Codex
- 修改摘要：支持在时间线任务上悬停编辑名称；桌面端侧边栏完全收起为零宽并让主内容扩展；将已有标签和标签入口移动到编辑器上传操作行，底部仅保留字数与自动保存状态。
- 影响范围：`src/main.jsx` 时间线任务编辑、文档标题更新和编辑器操作行；`src/styles.css` 侧边栏收起、任务编辑控件和顶部操作布局。

## 2026-09-02-11
- 修改人：Codex
- 修改摘要：将侧边栏主创建按钮的显示文案和悬浮提示从“新建项目文档”调整为“新建项目日程”，快捷键和创建逻辑保持不变。
- 影响范围：`src/main.jsx` 侧边栏主创建按钮文案与提示文本。

## 2026-09-02-11
- 修改人：Codex
- 修改摘要：移除侧边栏“新建项目日程”按钮及其占位空间，保留顶部菜单中的新建项目文档入口和现有创建逻辑。
- 影响范围：`src/main.jsx` 侧边栏创建入口与界面布局；`src/styles.css` 侧边栏按钮隐藏规则；不影响文档创建功能。

## 2026-09-02-11
- 修改人：Codex
- 修改摘要：移除“视图”菜单中的“显示侧边栏”和“隐藏侧边栏”入口，保留顶部侧边栏图标作为唯一切换方式。
- 影响范围：`src/styles.css` 视图菜单显示规则；侧边栏状态持久化和顶部切换按钮不变。

## 2026-09-02-11
- 修改人：Codex
- 修改摘要：移除项目文档统计卡片和“下一项”提示；在时间线任务时间信息行右侧增加删除按钮，并加入删除确认与删除后的自动选中逻辑。
- 影响范围：`src/main.jsx` 时间线任务删除、项目摘要移除和视图菜单清理；`src/styles.css` 删除按钮布局样式；文档删除后同步更新浏览器本地存储。

## 2026-09-02-11
- 修改人：Codex
- 修改摘要：将“编辑”菜单中的“新建项目文档”改为“新建项目日程”，点击行为和创建逻辑保持不变。
- 影响范围：`src/main.jsx` 顶部编辑菜单文案。

## 2026-09-02-12
- 修改人：Codex
- 修改摘要：按日期合并 `docs/history/` 中分散的历史文件，统一为每日一个历史文件，并保留全部已完成修改记录。
- 影响范围：`docs/history/` 文件组织和历史记录索引；不影响应用源码与运行时数据。

## 2026-09-02-14
- 修改人：Codex
- 修改摘要：为每个项目增加独立的“项目文档”和“项目排期”子入口；项目排期复用月历并仅显示当前项目日程，同时保留全局项目排期总览；补充项目级导航高亮、面包屑、空状态和日程回到对应文档的选中逻辑。
- 影响范围：`src/main.jsx` 项目导航、排期范围状态、Calendar 筛选与任务跳转；`src/styles.css` 项目子导航和日历空状态样式；不改变 `project`、`planned`、`plannedTime` 字段及本地存储结构。

## 2026-09-02-14
- 修改人：Codex
- 修改摘要：根据界面反馈移除项目行下方的文档/排期子入口，将顶部“视图”菜单改为“切换”，集中提供当前项目的项目文档与项目排期切换；保留侧边栏全局项目排期总览入口。
- 影响范围：`src/main.jsx` 项目导航与顶部切换菜单；`src/styles.css` 项目列表布局；不改变项目级排期筛选、任务回跳和本地存储结构。

## 2026-09-02-14
- 修改人：Codex
- 修改摘要：移除侧边栏底部的“项目文档”入口，将全局排期入口改名为“总项目排期”，并同步更新全局排期页面标题、说明和面包屑文案。
- 影响范围：`src/main.jsx` 全局侧边栏导航、Calendar 全局标题与说明；当前项目切换菜单及项目级排期逻辑保持不变。

## 2026-09-02-14
- 修改人：Codex
- 修改摘要：为项目文档时间线增加右侧可拖拽分隔条，支持左右调整时间线宽度并持久化；将标签入口升级为标签管理面板，支持新增、重命名和删除当前文档标签。
- 影响范围：`src/main.jsx` 时间线布局、宽度状态、标签增改删逻辑；`src/styles.css` 拖拽分隔条、标签面板和响应式样式；继续复用文档 `tags` 字段及本地存储。

## 2026-09-02-15
- 修改人：Codex
- 修改摘要：将顶部“编辑”下拉菜单改为直接编辑按钮，点击后立即进入当前文档编辑模式，移除新建、完成、上传和预览等下拉功能。
- 影响范围：`src/main.jsx` 顶部编辑导航；其他页面内新建、完成、上传和预览操作保持不变。

## 2026-09-02-15
- 修改人：Codex
- 修改摘要：将快捷键说明移入编辑菜单，并将顶部设置、编辑、切换入口统一调整为仅显示文字，移除入口图标与下拉箭头。
- 影响范围：`src/main.jsx` 顶部导航菜单与快捷键入口；侧边栏切换按钮及其他页面功能保持不变。

## 2026-09-02-15
- 修改人：Codex
- 修改摘要：移除顶部导航中的项目名称、斜杠和页面路径面包屑，保留设置、编辑、切换及右侧工具区域。
- 影响范围：`src/main.jsx` 顶部导航布局；项目切换、页面状态和侧边栏交互不变。

## 2026-09-02-16
- 修改人：Codex
- 修改摘要：敲定 DailyTime 桌面应用技术框架与分阶段实施方案，确定采用 Tauri 2 + React/Vite、SQLite 本地存储、应用数据目录附件、可选加密 `.dailytime` 备份，以及先发布 Windows 10/11 x64 的策略。
- 影响范围：新增桌面应用架构方案文档；更新 `docs/README.md` 文档索引；明确首版数据边界、迁移策略、测试门槛和跨平台发布约束，不改变现有运行时代码。
